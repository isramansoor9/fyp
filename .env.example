# Flask backend origin (no trailing slash). Required for production builds that call your API.
# Example: https://fyp-1xcl.onrender.com
# If unset, the app defaults to http://localhost:5000 for local development.
NEXT_PUBLIC_API_URL=
