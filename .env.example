# Flask API origin used by the Next.js app (see lib/backendUrl.ts).
# No trailing slash. Set on Vercel for production.
#
# Example production backend (update if your Render URL differs):
# NEXT_PUBLIC_API_URL=https://fyp-1xcl.onrender.com
#
# Local dev: omit this variable to default to http://localhost:5000
